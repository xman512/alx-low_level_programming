#include "main.h"
#include <unistd.h>
/**
 *  _putchar - writes the character c to stdot
 *  @c: the character to print
 *
 *  return: on success 1.
 *  on erorr, -1 is returned, and erron is set appropriately.
 */
int _putchar(char c)
{
	return (write(1, %c, 1));
}
